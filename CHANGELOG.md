# AniTracker 追迹 — 版本史（CHANGELOG）

从 readme.md 迁出（v2.3 ~ v2.12，2026-09-25 迁移）。此后新版本记录请直接追加到本文件。

## v2.3（2026-09-14）
- 集级来源标注（原作改编 / TV原创 / 半原创 / 未判定），支持从 Anime Filler Guide 数据批量补齐与人工标注；
- 来源筛选：主界面可隐藏 TV原创 / 半原创，「下一集」与统计联动，筛选不改变观看记录；
- 同步中心：主界面顶栏「同步」入口；Bangumi 双向同步（拉取 / 推送 / 台账 / 重试 / 回滚）、账号云同步、备份与导入统一入口；
- 版本对齐：tracker-version.json 与页面版本双向校验；详细文档见 docs/（判定口径 / 部署与回滚 / 验证记录）。


## v2.4（2026-09-14）
- 免绑定搜索：不再要求 token；本地库（离线可用）+ Bangumi 在线（实时、匿名可用）双源分区结果；
- 自带「＋ 手动添加」（零依赖）与「⌁ 关联Bangumi」（本地番补映射，之后可同步进度）；
- 点击即判重（归一化标题 / 同条目），拦截提示不产生重复数据；断网自动降级为本地结果；
- 添加/关联写入同步记录台账；最近搜索保留可清空；同步中心「高级设置」可一键回退旧行为；
- 详细说明见 docs/搜索与添加（免绑定）.md（含 HTML 版与验证记录-v2.4）。

## v2.4.4（2026-09-15）Bangumi 链路整改
- 未开播条目添加给出明确提示（不再误报网络问题）；网络类错误不再自动重试（断网报错 ~63s→~20s）；封面拉取加 6s 超时；404/绑定文案修正；bgmGet 死代码移除；
- 双入口收口：index.html 为唯一源，ani-tracker.html 为脚本同步副本（python 同步双入口.py），PWA start_url 改指 index.html；
- 新增回归测试：tests/run-regression.js（16 项断言，覆盖断网/限流/401/5xx/404 五类异常）+ 运行回归测试.bat；
- 完整交接：DELIVERY/bangumi-overhaul-20260915/（盘点/审计/裁决/变更回滚/验证/手册 + 总览报告 HTML）。

## v2.5.0（2026-09-15）季度秒切 + 同步摘除
- 季度切换重做（B站式）：详情页自动整合分季；未加入的季以「预览模式」浏览（缓存秒切，实测 17ms，绝不强制入库）；系列数据缓存 at_series_cache + 后台预取；
- 搜索结果按系列自动分组成季度 chip，点 chip 加入自动带整条季度条并预取；
- Bangumi 同步整体摘除（BGMSYNC=false 开关）：拉取/推送/自动同步/绑定全部停用，恢复改一行；搜索与剧集数据读取保留；
- 回归重写 12 项全绿；详见 DELIVERY/anitracker-v2.5.0-seasons-20260915/。


## v2.8.0（2026-09-24）同步修复 + TV原创数据集扩充

**修复「同步做不好」**
- 根因：`api.bgm.tv` 国内直连不通；原「公共 CORS 代理兜底」两个代理均已失效
  → 每次请求干等 13 秒后报一句含糊的「网络不可达」
- 直连超时 13s → 4s；移除已死代理；失败诊断改为「请开代理软件并启用系统代理/TUN」，可照做
- `BGMSYNC` 由 false 恢复 true，拉取/推送 Bangumi 按钮回归
- 新增可选「CORS 中继」设置（进阶）

> 认知修正：浏览器 `fetch` 无法指定 HTTP 代理，初版的「用户自填代理」方案已废弃。

**修复「TV原创标注做不好」**
- 根因：原数据集来自已停更的 animefillerguide.com，仅 58 部且不含柯南
- 改用 `animefillerlist.com`，全量爬取 **212 部**（+265%）：TV原创 3504 集 / 半原创 2610 集
- 柯南 575 集、死神 163 集、火影 90 集、海贼王 94 集 全部命中
- 别名表扩充至 80+ 条 + 中英文松散兜底匹配（`fgLooseMatch`）
- **新增联网实时抓取**（`fgFetchSlug`/`fgParseAfl`）：本地未收录的新番可一键抓取并缓存，
  解决「以后新番怎么办」

**测试**：项目回归 19/19 PASS；独立 Playwright 实测核心通过；双入口字节一致。
详见 `DELIVERY/anitracker-v2.8.0-sync-filler-20260924/`。

---

## v2.9.0（2026-09-24）· AI 结果导入

**解决什么**：v2.8.0 的 Filler 数据集只有 212 部（animefillerlist.com 全量），
**新番 / 冷门番依然查不到**，只能手动一集一集标。任何第三方数据集都追不上新番更新速度。

**方案**（用户提出）：先问 AI，再把 AI 的回答整段贴给项目。

**用法**：详情页 `✧ AI 结果导入` → `看看要 AI 怎么说`（自动生成提问话术）→ 把 AI 回答整段贴回 → `预览识别` → `写入`。
不用整理格式。

**能认的写法**（实测全过）：`1-10集为漫改，11-15集是TV原创，16集半原创` /
`TV原创：11-15, 23, 45-47` / `1,2,3集是原创` / `第 1、2、3 集是原创；第4集漫改` /
`ep1-5 manga canon, ep6 filler` / `原创：45-47 半原创：50` / `1~5集漫改`。
中英混排、全角半角、有无「第/集/话/ep」都认。

**冲突策略**：AI 粘贴的**优先**，覆盖自动校准；集级来源标为「AI 导入」。
但**你真正手改过的集不会被覆盖**。

**容错**：能认几条认几条，认不出的原样列出、不阻塞写入；纯叙述句静默忽略；
超范围集号预览时警告、写入时丢弃。

**实现**：新增 `at_ai_paste` 存储层（独立于自动抓取缓存，优先级最高）；
`srcInfoOf` 新增 `by:'ai'` 判定分支；解析器 `AT270.aiParse` 用「子句就近归属」
（逗号切子句 → 本子句有词用自己的 → 没有继承前句 → 前句也没有才向后借）。

**测试**：新增 `tests/ai-import-e2e.js`（12/12 PASS，含解析器 8 组单元 + 详情页真实交互）；
主回归 19/19 PASS；双入口字节一致。
详见 `DELIVERY/anitracker-v2.9.0-ai-import-20260924/`。

---

## v2.9.1（2026-09-24）· 同步精简总开关

**解决什么**：用户反馈「同步还是很累赘」（界面太重、按钮太多）。

**做法**：一个总开关，默认关闭。关闭后隐藏所有 Bangumi / 云同步相关 UI：
- 顶栏 `⇅ 同步` → 改名 **`⇅ 数据`**（保留入口，因为「本地备份」在里面）
- 详情页 `拉取Bangumi进度` / `推送Bangumi` / `关联Bangumi` → 全部隐藏
- 同步中心里的 Bangumi 绑定卡、同步记录台账、账号云同步、高级设置（代理/中继）→ 全部隐藏
- 自动推送变为**完全静默**（`bgmTok()` 返回空 → 15 个推送函数全部空操作，零请求零报错）

**保留**：`本地备份（导出/导入）`——纯文件操作，不碰网络，是真正防丢数据那层。

**开回**：`⇅ 数据` → 展开「已精简的同步功能」→ `重新开启同步功能`。反向同理（高级设置最底部）。

**实现要点**：默认精简（`localStorage.at_sync_off` 无值即精简）；**单点拦截**——
改 `bgmTok()` 一个函数覆盖 15 处调用点，避免改漏；`syncOpen()` 按 `OFF` 三元渲染。

**测试**：主回归 **22/22 PASS**（新增 T10a 默认精简 / T10b 精简面板内容 / T15b 精简态静默）；
AI 导入 12/12 PASS 未退化；双入口字节一致。
详见 `DELIVERY/anitracker-v2.9.1-slim-sync-20260924/`。

## v2.9.2（2026-09-25）· 同步改版：按使用频率分层

v2.9.1 的「精简总开关」是错的解法——它只是把累赘藏起来，不是消除累赘。
本版改为**频率分层**：常用的摆出来、不常用的收起来、一眼能看出状态。

- **顶部一句话状态**：`● Bangumi 已绑定 · 上次同步 1 小时前`，打开就知道什么状况
- **一个主行动按钮**，随状态变：未绑定 →「去绑定 Bangumi」；已绑定 →「立即同步进度」
- **常开档**：本地备份、Bangumi 账号卡
- **折叠档**（默认收起、点开即用、状态会记住）：账号云同步 / 同步记录 / 高级设置
- **详情页**：拉取/推送按钮改为**只在本作真的关联了 Bangumi 时出现**；
  日常按钮（播放/编辑集数/来源校准/AI 导入）排到前面
- 撤掉 `syncOff()` 全局隐藏、`applySyncOff`、`optSyncOn/optSyncOffBtn`；
  顶栏按钮从「数据」改回「同步」；`bgmTok()` 单点拦截移除，同步链路默认可用
- 老用户若留有 `at_sync_off` 标记，首次打开自动清除并恢复完整 UI

测试：`run-regression.js` 22/22 · `ai-import-e2e.js` 12/12

## v2.9.3（2026-09-25）· 纯记录定位

- **移除**详情页的「▶ 去门户播放」按钮及其 `playInPortal()` 函数
  —— 本作只做**观看记录的追踪**，不负责跳转播放
- 门户相关的「B站追番导入」保留（那是**导入**，不是播放）
- 详情页按钮区现为：`编辑集数 / 来源校准 / AI 结果导入 / 重拉封面`
  （+ 本作关联了 Bangumi 时才出现的 拉取/推送）

## v2.10.0（2026-09-25）· 方向转向「账号」

**把「同步」拆开：删掉进度同步与 WebDAV，把账号系统扶正。**

顶栏入口按钮：**「⇅ 同步」→「☺ 账号」**。面板重排为：

- **状态条**：`● 已登录：xxx` / `○ 未登录 · 片单只存在本机浏览器`
- **账号**（常开、第一屏）：登录 / 注册（邮箱 + 密码 → 邮箱验证码 → 建号）
- **本地备份**（常开）：导出 / 导入一个文件，不依赖账号
- **高级设置**（折叠）：免绑定搜索 / 代理说明 / CORS 中继

删掉的东西：

- **Bangumi 观看进度双向同步** —— 拉取/推送/台账/回滚/绑定卡/同步记录，
  连同 `BGMSYNC` 开关、`syncOff`/`syncFoldPref`/`syncHealth` 状态机
- **WebDAV 文件同步** —— `syncRun()` / `tracker-sync.json` / `tr_meta` /
  6 分钟后台定时器 / 配置面板（顺带清掉 `btoa` 明文存密码的隐患）
- 详情页的「⟳ 拉取Bangumi进度」「↑ 推送Bangumi」两个按钮

**保留边界（关键）**：Bangumi 的**导入 / 关联**链路完整保留 ——
`bgmSid` `bgmQueue` `bgmFetch` `bgmPullEpisodeList` `bgmFillEpisodes`
`bgmAttach` `bgmAttachGroup`。它们也读 token，但用途是「把番和剧集列表
填进片单」，不是「同步观看进度」。**`bgm*` 前缀不能通配删**，删了搜索添加
和关联 Bangumi 会一起坏。测试 `T10c` 专门守这条线。

**顺带修的 bug**：`CBSync.isOn` 未导出，导致状态条永远显示「云同步未开」
（调用被 try/catch 静默吞掉）。已补导出 `isOn()` / `current()`。

数据仍在**腾讯云开发 CloudBase**（环境 `cloud1-d7gsn5t0w6407b963`，ap-shanghai），
按 `ownerId` 隔离；key 是 `PublishableKey`，可安全放前端。

测试：`run-regression.js` **23/23 PASS** · 双入口 md5 一致

## v2.10.1（2026-09-25）· AI 结果导入面板重做

**从三步表单变成粘贴即出结果。解析器 `aiParse` 一行未改 —— 只动交互与视觉。**

- **粘贴即识别**：撒掉「预览识别」按钮。`input` 防抖 260ms、`paste` 走 60ms 快速通道，
  贴完立刻看到认出了什么
- **三行灰字 → 分类色卡**：`● TV原创 11-15 (5 集)` 一行一色，集数右对齐独立成列
- **占比条**：三类各占一段，一眼看出「这作是漫改为主还是原创为主」
- **未识别的行收进折叠区**：不再和结果混在正文里
- **超范围集号明说**：「有 N 个集号超出本作范围（共 M 集），不会写入」，不再静默丢弃
- **按钮 5 → 3**：只留「复制提问给 AI」（+ 有旧结果时的「清除已存结果」）
  和底部「取消 / 写入」，「写入」禁用时有明确视觉态
- **配色跟随主题**：TV原创=赤陶 / 半原创=赭黄 / 漫改=松绿，
  深色主题下换同色系降饱和版本；底色描边由 `color-mix` 从同一色变量算出

**修掉两个真 bug**：

1. **面板关闭后防抖定时器仍触发 → 读 null 崩溃**
   （回归 `A12` 报 `pageErrors=1`：`Cannot read properties of null (reading 'value')`）。
   修法：`showPreview()` 开头先判 `$('at270AiText')` 与 `wrap.parentNode` 存活。
   这个 bug 只在「粘贴 + 快速关闭」的时序下出现，手点几下撞不到。
2. **toast 浮层遮住面板底部「写入」按钮**（`#toast` 是 `bottom:34px` 固定定位）。
   修法：新增 `body.panel-open #toast{bottom:96px}` + 在 `document.body` 挂**一个**
   `MutationObserver`，只认「页面还有没有 `.mask`」——以后新加面板自动生效，不用逐个接线。

**测试**：`run-regression.js` **23/23 PASS** · `ai-import-e2e.js` **12/12 PASS**
（A5 从正则匹配整段文本改为**直接读色卡 DOM** 的 `.lbl/.range/.cnt`，
固定 `sleep` 全换 `waitForFunction`）· 双入口 sha256 一致（`66473dc30c655c9b`）

详见 `DELIVERY/anitracker-v2.10.1-ai-import-20260925/`。

---

## v2.11.0（2026-09-25）· 登录 / 注册重做（参考 B 站双 Tab）

**原来「注册」是登录框下面一行小字「注册一个 →」，点开是个没反馈、没防重、刷新就丢的表单。
现在它和「登录」平级成两个 Tab，注册分两步：① 填邮箱密码 ② 去邮箱收验证码。**

- **双 Tab**：`登录 | 注册`，当前项下划线高亮，进入即聚焦第一个输入框
- **注册两步走**：带编号圆点的 `① 填邮箱和密码` / `② 去邮箱收验证码（10 分钟内有效）`；
  验证码输入框与「获取验证码」**同一行**，符合直觉
- **可选的用户名收进折叠档**：`＋ 顺便设个用户名（可选）`，不打扰主流程
- **验证码文案与校验统一为 4–8 位**（旧版提示写死「6 位」，与校验正则不一致）

**修掉 5 个真缺陷**（全部先用真实浏览器打真实 CloudBase 拿到证据，不靠推测）：

1. **可重复发信**。旧代码守卫 `if (btn.disabled) return;` 是**死代码** —— `btn.disabled` 在
   `await signUp` 期间从未被置 true。实测 `signUp` 要 **5237 / 5802 ms**（真 SMTP 往返），
   这 5 秒多里按钮是「可用」的，连点就连发。
   → 改为点击瞬间**同步**锁按钮（放在 `await` 之前）+「发送中…」+ `_sending` 重入锁。
2. **刷新即丢验证凭证**。`verifyOtp` 句柄只存在模块级闭包里，没有明文可存
   （实测 `su.data.verification_id === undefined`），刷新/切后台就丢 —— 而正常流程恰恰
   要去邮箱收信几十秒到几分钟。
   → 用 `auth.resend` 取回可持久化的 `messageId`（JWT，`{e, exp:+10min, pj}`）存
   `localStorage['at_reg_pending']`，刷新后用 `auth.verifyOtp({token, messageId})` 重建，
   并自动跳回注册页、预填邮箱、顶部状态条提示「还差一步」。
3. **发信过程零反馈**。加了「发送中…」态、成功提示（含「可能在垃圾箱」）、失败按类型给可读文案。
4. **「顺便设用户名」永远无效**。`#cbNewName` 在代码里**只被读过、从未被渲染**
   （`grep -c cbNewName` = 1，那 1 处还是 reader）。→ 真正渲染出来，并抽出 `bindUsername()` 复用。
5. **错误文案靠撞中文、还会吐乱码**。真实错误对象里 `message` 是
   `无效的验证码%!(EXTRA string=rpc error: ...)` —— `%!(EXTRA …)` 是 **Go `fmt` 格式化残渣**。
   → 改为**结构化字段优先**（`category` / `status` / `errorCode`），并用 `cleanMsg()` 砍掉残渣。

**一个差点写错的地方**：修 `errText` 时我第一版对**全字段拼接串**做 `/过期|失效/` 匹配，
但实测 `helpMessage` 是一份**「常见原因清单」**，里面同时列着「验证码已过期」和
「验证码输入不正确」—— 拿它匹配必然把**填错码**误判成**已过期**。纯逻辑测试 `X1` 把这条抓了出来。
正解：只认 `message` / `status` / `error_description` 这三处结论性文本，不碰 `helpMessage`。

**测试**：`register-logic.js` **55/55** · `register-e2e.js` **13/13** · `run-regression.js` **23/23**
（结构法：先毫秒级纯逻辑用真实错误对象喂 `errText`，再上真 Chrome E2E，最后跑项目自带回归）
· 双入口 sha256 一致（`ffbe658f06a8d11a`）

详见 `DELIVERY/anitracker-v2.11.0-register-20260925/`。

**未做**：手机号 + 短信注册（短信签名/模板需另行报备），当前保持邮箱注册。

## v2.12.0（2026-09-25）· 右键菜单 + 重复番剧检测

### 右键菜单（片单卡 / 搜索结果卡 / 剧集行，三处都挂）

桌面右键、手机长按 500ms 双通道（移动 >12px 取消长按；长按后吃掉紧随的 click，不误跳详情页）。

| 挂载点 | 菜单内容 |
|---|---|
| 片单卡片 | 打开详情 · 标记下一集已看 · 改为「想看/在看/看完/搁置/抛弃」（当前态不重复列出）· 复制番名 · **从片单移除** |
| 搜索结果卡 | 加入片单 · 打开详情 · 复制番名 |
| 剧集行 | 标记已看/取消已看 · 标记重看 · **标记到这一集为止**（批量，带确认）· 改来源标注 · 复制集名 |

菜单自动避让视口边缘、`Esc`/滚动/点别处关闭、方向键可移动（键盘可达）、文案全走 `esc()`。

**踩到的坑**：项目里有**两个 `renderGrid`**——原始版（~1290 行）和 `AT270` 覆盖版（~3460 行），
**运行时生效的是后者**。剧集行的 `data-ep` 第一次加错位置，等于没改且不报错，靠 E2E 报「no eprow」才发现。

### 重复番剧检测（账号面板 → 片单整理）

按「标题归一化」+「Bangumi ID」双路查重。关键在于**同名组要再按「年份+集数」细分**——
否则 `海贼王 1999·1168集` 和 `海贼王 剧场版 2000·41集` 这种「都是真的、谁也不该删」的组合会被列进可移除清单。

报告分三段，只有第一段给「移除」按钮：

| 段落 | 判据 |
|---|---|
| 发现 N 组疑似重复 | 标题、年份、集数都一样（第一条标「建议保留」） |
| 另有 N 组同条目不同标题 | 同一 Bangumi 条目、标题不同（提示人工确认） |
| 同名但不同的作品 · N 组 | 年份/集数不同，**或标题是前缀关系**（无移除按钮，写明「不是重复数据，无需处理」） |

**前缀关系这条是后来补的、也是直接回答用户问题的**。第一版只比对「归一化后完全相同」，
而 `海贼王` 与 `海贼王 剧场版` 归一化后是 `海贼王` vs `海贼王剧场版`，**并不相等** ——
于是报告显示「✓ 没有发现重复番剧」，可界面自己的说明文字还写着「如『海贼王』TV 与『海贼王 剧场版』」，
**文案在撒谎**。截图审查时发现。补法：两两比对，一方以另一方开头（短的 ≥ 2 字符）即收进该段。

### 顺手修的真实缺陷：`setEpStat` 在 `statuses` 缺失时崩溃

`epStat` 读的时候是安全的（`(s.statuses||{})[n]`），但 `setEpStat` **写的时候没有兜底**：

```js
if(st===null) delete s.statuses[n]; else s.statuses[n]=st;   // s.statuses 可能是 undefined
```

历史版本数据、半写入、外部导入的残缺记录都会缺 `statuses`，这类记录**第一次标记第一集就抛**
`TypeError: Cannot set properties of undefined`。发现路径：E2E check 14 一直失败
（菜单关了但一集都没标记上），异常被调用链吃掉，表面只剩「什么都没发生」，加探针才定位到。

→ 前置 `if(!s.statuses) s.statuses={};`；`cntWatched`/`cntRewatch` 同步改为 `(s.statuses||{})` 读。

**测试**：`ctxmenu-logic.js` **56/56** · `ctxmenu-e2e.js` **25/25** · `run-regression.js` **23/23**
· 双入口 sha256 一致（`4ee30ed8650b5ebf`）
详见 `DELIVERY/anitracker-v2.12.0-ctxmenu-20260925/`。

## v2.13.0（2026-09-25）安全与工程化大修

对全项目 21 项缺点审计后的集中修复（build 20260925h）。

**安全**
- 备份/云同步凭证隔离：导出与上传剔除 `at_bgm_token` / `at_net_relay` / `credentials_*` / `tr_dav`；
  导入与云端恢复全部过 `backupSanitize`（键名白名单正则 + 值类型/长度校验 + sid 归一），
  堵住「恶意备份文件改写 CORS 中继地址→外发搜索词」的投毒通道。
- `esc()` 补转义单引号；搜索卡 `d.id` 强制数字校验，收口字符串拼 `onclick` 的 XSS 逃逸面。

**云同步不再互丢**
- 整包覆盖 → 逐剧合并：`s.updAt` 时钟比字段、观看历史并集去重（保留最近 200 条）、
  删除墓碑 `at_tomb`（500 环）防「删了又漂回来」、`addedAt` 取小。
- 三个合并入口：开机自动合并（sessionStorage 守卫防刷新循环）、upload 先合后传、手动「从云端合并」。
- `cbUp` 去掉「覆盖云端」确认——先合后传后双方修改都保得住。

**体积与离线**
- 302 部内置数据集（649KB）外置 `ani-tracker-lib.json` 懒加载，HTML 928KB→约290KB；
  装载后按当前视图条件水合，不劫持界面。
- SW 升 v12 分层缓存：页面/版本文件 network-first，大件（lib/SDK/filler 数据）stale-while-revalidate + 预缓存。
- cloudbase SDK 落到本地 `vendor/cloudbase.full.js`，CDN 失败自动兜底，离线可用。

**可靠性**
- 错误留痕 `at_errlog`（环形 100 条）+ 全局 `error`/`unhandledrejection` 兜底。
- AI 导入冲突集显式上报（不再静默跳过），数据质量面板显示「含 AI 导入 N 部」。
- 删除封面链路中两个已失效的公共代理（proxy.cors.sh / codetabs）。

**UI/交互**
- 11 处原生 `confirm`/`prompt` 全数换主题弹窗 `uiDialog`（焦点圈定 / Enter 提交 / Esc 取消 / aria-modal），
  手动添加三连环 prompt 合并为三输入一屏。
- 新增 `--ok/--info/--warn` 令牌并统一 10 处硬编码成功色；`--muted` 对比度提到 AA；
  toast 挂 `role=status aria-live`；新增 900/1300px 平板桌面断点（正文 720/820px 居中）。

**走查中新修的真实缺陷**
- `CBSync.current()` 自 v2.10.0 起返回 Promise，而 `acctStatus()` 同步判真值——Promise 恒为真，
  导致**未登录也永远显示「已登录」状态条**。现 `sess()` 结果同步缓存，`current()` 直读缓存，退出即清。

**工程**
- 测试依赖仓库内化（puppeteer-core + lock）、`AT_PY/AT_CHROME` 环境变量探测、测试路径去绝对化（15 文件）；
- `.gitignore` 规则改正、`__pycache__` 退库、启动链收敛（孤儿脚本入退役夹）、
  双入口 pre-commit 钩子（`core.hooksPath=.githooks`）、版本史迁本文件。

**测试**：`v2130-checks.js` 26/26 · `ctxmenu-logic.js` 56/56 · `run-regression.js` 23/23 · `register` 55/55 ·
浏览器 E2E 走查（深浅双主题 / 主题弹窗 / 手动添加草稿恢复 / 账号面板 vendor 装载 / 1280px 断点实测 620px 收口）。
