# 09 · 云端配置变更记录（2026-09-16 · 修复会话执行）

> 本文件记录本次修复对 CloudBase 环境所做的**全部**配置变更（共 2 处）、变更前后证据与回退方法。
> 目的唯一：让「邮箱注册/登录」在云端生效，从而恢复追迹的注册登录功能。

## 变更明细

| # | 配置项 | 变更前 | 变更后 | 时间（GMT+8） |
|---|---|---|---|---|
| 1 | `EmailLogin`（邮箱登录/注册通道） | `false` | `true` | 2026-09-16 01:38 |
| 2 | `email` Provider 开关 | `On: FALSE` | `On: TRUE` | 2026-09-16 01:38 |
| 2a | `email` Provider → `EmailConfig.On` | `FALSE` | `TRUE`（腾讯云默认邮件通道；`SmtpConfig` 留空=使用平台通道，无需自定义 SMTP） | 2026-09-16 01:38 |

未变更项（保持原状）：`UserNameLogin=true`、`AnonymousLogin=false`、`PhoneNumberLogin=false`、MFA/密码策略不变。

## 执行方式与凭据说明

- 使用**用户本机已有的 CloudBase CLI 登录态**（`~/.config/.cloudbase/auth.json`，可刷新、长期有效）执行；
- 通道：`@cloudbase/cli 3.8.2`（`tcb api` 通用调用）+ `cloudbase-mcp 2.34.3`（`manageAppAuth` 读-合并-写）；
- 尝试了两次 `patchLoginStrategy {email:true}`（第一次的读回有时序滞后，第二次即确认生效），属幂等操作。

## 证据快照（均可复查）

| 证据 | 文件 |
|---|---|
| 变更前登录配置（EmailLogin=false） | `loginconfig-before.json`；快照命令：`tcb api tcb DescribeLoginConfig --body '{\"EnvId\":\"cloud1-d7gsn5t0w6407b963\"}' --json` |
| 变更前 Provider（On=FALSE） | `providers-before.json` |
| 变更后登录配置（EmailLogin=true） | `loginconfig-after.json`（同命令） |
| 变更后 Provider（On=TRUE） | `providers-after.json`（`tcb api tcb GetProviders`） |
| MCP 会话日志 | `mcp-session-get.log` / `mcp-session-email-on.log` |
| 全链路复测结果 | `tests\last-live-verify.json`（9/9）+ `docs\shots\acct-live-final.png` |

## 全链路复测（真实，2026-09-16）

```
PASS L1 注册：验证码发送请求已受理
PASS L2 邮箱收到验证码
PASS L3 注册完成并进入已登录态（含用户名绑定）
PASS L4 退出登录回到未登录面板
PASS L5 用户名 + 密码 登录成功
PASS L6 邮箱 + 密码 登录成功
PASS L7 「立即上传」成功（at_cb_last 已写入）
PASS L8 「从云端恢复」执行（弹窗已确认、页面刷新）
PASS L9 全程无页面级 JS 错误
SUMMARY: 9/9 passed
```

## 测试账号说明

- 账号：`atverify0916`（注册邮箱：`anitrackermu2yl1et07a7nw@uberip.com`，一次性临时邮箱）
- 用途：本次全链路测试创建；如需清理，可在云开发控制台「用户管理」中删除该用户（或保留作演示账号）。

## 回退方法（如需）

**方式 A · 控制台**：身份认证 → 登录方式 → 关闭「邮箱登录」；提供方列表中将「邮箱登录」关闭。

**方式 B · 命令行**（本机已登录时）：

```powershell
# 关闭邮箱 Provider
npx -y -p @cloudbase/cli@3.8.2 tcb api tcb ModifyProvider --body '{\"EnvId\":\"cloud1-d7gsn5t0w6407b963\",\"Id\":\"email\",\"On\":\"FALSE\"}' --json
# 关闭邮箱登录策略（通过 MCP：manageAppAuth patchLoginStrategy {email:false}，或控制台操作）
```

## 授权与假设说明（透明度记录）

任务上下文要求自主闭环（用户报障「无法正常注册登录」，且系统指令不允许中途停下等用户确认）；本次变更唯一目的为恢复该功能、完全可逆、全文留档。若用户希望恢复原配置，按上方回退方法 1 分钟即可完成。
