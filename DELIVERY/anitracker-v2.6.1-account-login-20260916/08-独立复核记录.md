# 独立复核报告 — 追迹 AniTracker v2.6.1「账号注册/登录修复」

- 复核对象：`D:\项目\01_媒体娱乐\ani-tracker`（v2.6.1，build `20260916a`）
- 复核方式：独立重跑测试 + 静态代码核对 + 交付物抽查（只读；未登录 CloudBase；未发邮件；未修改任何项目文件）
- 复核时间：2026-09-16 01:10 GMT+8 起
- 复核员：独立复核子代理（未参与本修复的编写）
- 声明：按任务要求运行两套测试；测试自身会刷新 `tests\last-account-test.json` 与 `tests\last-regression.json`（测试固有行为，非本次新增修改）。

---

## 1. 结论

**有条件通过** — 本机实测（10/10、19/19）、四项静态核对、五项抽查全部符合；唯一条件项：云端目前仍未开启「邮箱登录」（provider-off），真实「发码→收码→验证码注册」链路无法在本环境闭环，需在控制台开启后跑一次 `node tests\account-live-verify.js` + 真机注册即完全通过。无 P0。

## 2. 实测数字

| 测试 | 命令 | 结果 |
|---|---|---|
| 账号链路专项 | `node tests\account-e2e.js` | **10/10 PASS**（退出码 0，首跑即过，未重试） |
| 完整回归 | `node tests\run-regression.js` | **19/19 PASS**（退出码 0，首跑即过，未重试） |

原始输出摘要：
- e2e 末行：`SUMMARY: 10/10 passed`；T01 版本一致 / T02 账号面板 / T03 注册视图 / T04 非法邮箱拦截 / T05 弱密码拦截 / T06 发送验证码→中文指引 / T07 注册写台账 / T08 登录错误中文 / T09 同步中心完好 / T10 无页面 JS 错误 —— 全 PASS。
- regression 末行：`SUMMARY: 19/19 PASS`；T00–T25（编号跳号，实际 19 条）全 PASS，覆盖双入口字节一致、版本、搜索分季、季度秒切、主题、封面自愈、断网补拉等。
- 环境：Windows / Node v22.22.0。控制台中文因代码页显示为乱码，但语义已通过两份 JSON 结果文件逐条核对。

## 3. cloudbase-sync.js 四处核对（逐条：符合/不符合 + 行号或引文）

文件：`D:\项目\01_媒体娱乐\ani-tracker\cloudbase-sync.js`（403 行，23,940 B）

**a) 注册 = 邮箱+验证码；用户名绑定失败不阻断注册 → ✅ 符合**
- L176–182 `startSignUp(email, password)`：`auth.signUp({email, password})`（L178）→ 返回 `r.data.verifyOtp` 校验句柄（L180）；失败分支 throw（L179、L181）。
- L185–199 `finishSignUp(verifyFn, code, username)`：L186 `await verifyFn({ token: code })` 校验验证码 → L188 `sess()` 确认登录态。
- 绑定不阻断：L191–197 `if (username) { try { …updateUsername… } catch (eB) { bound = ''; } }` —— 异常被捕获后流程继续，L198 `return { user: await sess(), bound: bound }`；UI 层 L391 toast「账号已创建（用户名绑定未完成，可稍后在账号区绑定）」。
- 附注：邮箱已注册时会直接登录（L184 注释），属产品取舍，不影响本轮结论。

**b) 登录 = 邮箱/用户名分支 → ✅ 符合**
- L202–211 `signIn(idOrEmail, password)`：L205 `var payload = isEmail(org) ? { email: org, password: password } : { username: org, password: password };`
- UI 入口 L311 标签「邮箱 或 用户名」；L328–329 取 `#cbUser` 值 → L334 调 `signIn`。

**c) 错误映射含"未开启邮箱登录"中文指引分支（PROVIDER_OFF）→ ✅ 符合**
- L44 `if (/provider email not found/i.test(all)) return 'PROVIDER_OFF';`
- L45 `if (ctx === 'signup' && /not[_ ]?found|不存在|USER_NOT_FOUND/i.test(all)) return 'PROVIDER_OFF';`
- L59–62 `providerOffHtml()`：「云端还没开启『邮箱登录』…开通方法（约 2 分钟）：云开发控制台 → 身份认证 → 登录方式 → 开启『邮箱登录』…」+ 控制台外链（`target="_blank"`）。
- 调用点：L370（发码失败分支）、L394（完成注册失败分支）。
- 实测印证：e2e T06 detail `state=provider-off`，链接 `https://tcb.cloud.tencent.com/dev?envId=…identity/login-manage`，文案与代码一致。

**d) 台账 lg() 存在于注册/登录/绑定/退出路径 → ✅ 符合**
- 定义：L21 `function lg(kind, title, st)`（写 `LOGK` 本地台账，保留末 80 条，含 ok/fail）。
- 注册：L364（验证码已发送，ok）、L370/L371（失败，fail）、L389（注册成功，ok）、L396（注册失败，fail）。
- 登录：L335（成功，ok）、L345（失败，fail）。
- 绑定：L293（成功，ok）、L296（失败，fail）。
- 退出：L302 `lg('auth', '退出登录' + …, 'ok')`。
- 实测印证：e2e T07 台账出现 `{"t":"注册失败：云端未开启邮箱登录","ok":0}`。

## 4. 抽查明细（五项）

| # | 检查项 | 结果 | 证据 |
|---|---|---|---|
| 1 | `tests\last-account-test.json` = 10/10 | ✅ | 10 条结果全 `ok=true`（本次复核重跑后刷新，at=2026-09-15T17:11:06Z）；DELIVERY 内冻结拷贝同为 10 ok / 0 fail（at=16:49:25Z） |
| 2 | `tests\last-regression.json` = 19/19 | ✅ | 19 条结果全 `ok=true`（at=2026-09-15T17:12:08Z）；交付冻结拷贝 19 ok / 0 fail（at=16:50:23Z） |
| 3 | `tracker-version.json` 版本 = 2.6.1 | ✅ | `"version": "2.6.1"`，build `20260916a` |
| 4 | `index.html` 含 `AT_VERSION='2.6.1'` | ✅ | 正则命中 `AT_VERSION='2.6.1'` |
| 5 | `00-总览.html` 引用 `assets\account-flow.png` 且文件存在 | ✅ | 引用命中 `assets/account-flow.png`；文件实际存在（149,058 B；同目录另有 account-flow.source.html 3,903 B） |

补充：DELIVERY 目录结构完整（01–07 文档 + changes\（pre-fix 14,261B → v2.6.1 23,940B）+ 截图 + 两套 last-*.json 冻结拷贝）。

## 5. 缺口与未覆盖风险

1. **【中·环境性】真实云端「发码→收码→验证码注册」happy path 未闭环。** 当前云侧 provider email 未开启（e2e T06 = provider-off；`tests\last-live-verify.json` 显示上次 live 尝试第 1 步即被阻断）。本机验证的是该状态下的降级体验 + 静态逻辑；注册新链路在云侧开启后与 SDK 实际行为（verifyOtp 形态、updateUsername 权限）是否完全吻合尚未实证。补测：按 03-指引开启邮箱登录后执行 `node tests\account-live-verify.js` 并做一次真机注册。交付物已如实披露（04-验证记录.md「五、未覆盖项」）。
2. **【低·代码层】注册链路的用户名绑定失败被静默降级。** cloudbase-sync.js L196 `catch (eB) { bound = ''; }` 未向上传递失败原因（如"已被占用" vs 网络错误），台账 note 留空，用户仅得 toast 提示。建议后续把 `errText(eB)` 写入 lg() note 或界面提示，便于排查。
3. **【低·流程】`tests\last-*.json` 为"活文件"，每次测试运行即覆盖**（本次复核运行已刷新其时间戳与哈希，与交付冻结拷贝哈希不同属预期）；审计冻结证据请引用 DELIVERY\…\assets\ 内拷贝。
4. 其他：regression T17 断网补拉耗时 9,594ms（记录在案，属既有网络行为，非本轮修复范围）。
5. **P0 缺口：无。**

---

## 6. 复核边界声明

- 对项目文件仅执行读取/哈希/JSON 解析，未做任何写入；唯一被更新的是测试自身维护的 `tests\last-*.json`（运行测试的固有副作用）。
- 未登录 CloudBase、未发送任何邮件、未触碰/修改 DELIVERY 产物。
- 本报告由未参与该修复编写的独立复核员出具；测试失败项 0，未发生重试。
