# 04 · 验证记录（2026-09-16）

> 原则：只记录真实执行过的测试；未执行 / 未覆盖项明确列出，不冒充已通过。

## 一、账号链路专项测试（`tests/account-e2e.js`）

执行命令：`node tests\account-e2e.js` ｜ 结果文件：`tests\last-account-test.json` ｜ 截图：`docs/shots/acct-reg-off.png`

**结果：10/10 PASS**（首跑 2026-09-16 00:47；云端启用后 01:44 复跑仍 10/10，T06 已走「已开启」分支实际发码成功）

| # | 用例 | 结果 | 关键证据 |
|---|---|---|---|
| T01 | 版本一致（页面 2.6.1 / CBSync 2.6.1） | ✅ | `{"a":"2.6.1","cb":"2.6.1"}` |
| T02 | 账号面板渲染（登录视图 + 注册入口） | ✅ | cbArea/cbUser/cbLogin/cbToReg 均在 |
| T03 | 注册视图展开（邮箱/验证码/完成按钮） | ✅ | cbEmail/cbCode/cbSendCode/cbFinish 均在 |
| T04 | 非法邮箱被本地拦截 | ✅ | 提示「请先填写正确的邮箱地址」 |
| T05 | 弱密码被本地拦截（需字母+数字） | ✅ | 提示「密码需 8–32 位，且同时包含字母和数字」 |
| T06 | 发送验证码（未开启态给指引；已开启态发码） | ✅ | 现在 `state=enabled`（实际发码成功） |
| T07 | 注册尝试写入同步台账（kind=auth） | ✅ | `at_sync_log` 新增 auth 记录 |
| T08 | 不存在账号登录 → 中文错误提示 | ✅ | 提示含「不正确/不存在/未注册」 |
| T09 | 同步中心其他区块完好 | ✅ | 同步记录、本地备份均在 |
| T10 | 无页面级 JS 错误 | ✅ | `[]` |

## 二、完整回归测试（`tests/run-regression.js`）

执行命令：`node tests\run-regression.js` ｜ 结果文件：`tests\last-regression.json`

**结果：19/19 PASS**（双入口字节一致、版本一致、搜索分季、季度秒切、主题、封面自愈等全绿）

## 三、回滚演练（真实执行）

执行时间：2026-09-16 ｜ 记录文件：`_cb_fix_20260916\rollback-drill-v261.txt` ｜ 演练目录：`_rollback_drill_v261\`

| 步骤 | 校验项 | 结果 |
|---|---|---|
| 基线（修复版） | `?v=20260916a` / `AT_VERSION 2.6.1` / version.json 2.6.1 | ✅ 全 True |
| 回滚（旧版文件切入） | `?v=20260914a` / `AT_VERSION 2.6.0` / version.json 2.6.0 | ✅ 全 True（切换耗时 29 ms） |
| 恢复（新版切回） | `?v=20260916a` / version.json 2.6.1 | ✅ 全 True（切换耗时 22 ms） |
| 恢复完整性 | index.html / cloudbase-sync.js / tracker-version.json SHA256 与暂存版一致 | ✅ 全 True |

> 说明：演练共执行两次，第一次因测量脚本读取方式问题（PowerShell 数组比较缺陷）误报失败，修正测量方式后重跑并全部通过；两次演练中真实文件切换本身均成功，最终态为修复版（哈希校验通过）。

## 四、真实云端负路径证据（修复前/未开启态）

| 项 | 证据 |
|---|---|
| 修复前：应用内注册失败原文 | `注册失败：You must provide either an email or phone number`（截图 `probe2-failure-before.png`） |
| 未开启态：界面中文指引 + 控制台外链 | 截图 `assets/acct-reg-off.png` |
| 云端 provider 未开启（原始响应） | `{"code":"NOT_FOUND","error":"not_found","error_code":5,"error_description":"provider email not found from endpoint"}` |
| 登录接口连通性 | `POST /auth/v1/signin` 正常返回 `invalid_username_or_password`（接口层健康） |

## 五、全链路真实复测（2026-09-16 01:42 · 已通过）

云端启用「邮箱登录」+ 默认邮件通道后（见 09 号文档），执行 `node tests\account-live-verify.js`：

**结果：9/9 PASS**

```
PASS L1 注册：验证码发送请求已受理
PASS L2 邮箱收到验证码（code=162450，真实到达临时邮箱）
PASS L3 注册完成并进入已登录态（含用户名绑定）
PASS L4 退出登录回到未登录面板
PASS L5 用户名 + 密码 登录成功
PASS L6 邮箱 + 密码 登录成功
PASS L7 「立即上传」成功（at_cb_last 已写入）
PASS L8 「从云端恢复」执行（弹窗已确认、页面刷新）
PASS L9 全程无页面级 JS 错误
```

证据：`tests\last-live-verify.json`、`docs\shots\acct-live-final.png`、`_cb_fix_20260916\live-verify-run1.txt`。

## 六、剩余未覆盖项

| 项 | 原因 | 建议 |
|---|---|---|
| 真机（手机端）注册 | 手机端与桌面端共用同一静态页面，未做真机走查 | 手机浏览器打开入口走一次注册（约 30 秒） |
| 平台侧用户视图人工抽查 | 控制台「用户管理」未纳入自动化 | 需要时可人工查看（测试账号 `atverify0916` 可见） |

## 七、环境与工具

- 浏览器自动化：Chrome 153 + puppeteer-core（无头模式）
- 静态服务：`服务器-空闲自退.py`（8094/8095/8096/8097 各测试端口）
- 云端管理通道（用于本次云端启用与复查）：`@cloudbase/cli 3.8.2`（`tcb api`）+ `cloudbase-mcp 2.34.3`（`manageAppAuth` / `queryAppAuth`），详见 09 号文档
- 网络：真实请求直达 CloudBase（`*.api.tcloudbasegateway.com`），未使用代理或模拟数据
