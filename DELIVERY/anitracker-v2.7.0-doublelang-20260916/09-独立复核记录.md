# 09 · 独立复核记录（v2.7.0 双语档案与防丢草稿）

## 一、复核方式

- 独立复核子代理（未参与开发）执行；首轮运行 20 分钟超时，返回**部分结论**；
- 其余复核项由主线补位完成，并全量重跑测试（下表）。

## 二、复核发现与处置

| # | 级别 | 发现 | 处置 |
|---|---|---|---|
| 1 | P2 | 登录/注册成功后，密码草稿不会自动清理（`clearDraftFor` 仅覆盖手动添加弹层） | **已修复**：草稿清扫器（6 秒周期）——弹层内表单消失（成功态重绘）即自动清理对应草稿；修复后 28/28、10/10 复跑通过 |
| 2 | 核验项 | `addInternal` 是否返回 Promise（模块对其使用 `.then`） | 已核实：`async function addInternal` 成立；另补充 `.catch` 异常兜底（加入失败有 toast，不再静默） |

## 三、主线补位核验（其余清单项）

- 测试三套实际复跑：**28/28、10/10、19/19**（`tests/last-v270-run.txt`、`tests/post-v270-account-e2e.txt`、`tests/post-v270-regression.txt`）；
- HTML 内联脚本语法：8 段 0 错误；双入口字节一致（同步输出 hash MATCH）；
- 版本三处一致：index.html `AT_VERSION='2.7.0'`、tracker-version.json 2.7.0、tests 断言 2.7.0；
- 文档一致性抽查：05 号文档数字与 assets 证据文件一致；04 号手册 AFG 映射与 `afg-one-piece-parsed.json`（f=115/m=43）一致；00 总览图片引用存在（渲染检查通过，`assets/overview-desktop.png`）；
- 静态审查要点：草稿白名单与清理语义、联想委托不与既有搜索冲突、editSrc 保留来源语义且集名仅作用单集、renderGrid 的隐藏/筛选/分页与原版一致、详情增强箱 int:/片单两条路径。

## 四、结论

**通过**（P2 已修复并复测通过）。

## 五、遗留与风险

1. [低] 真机（手机端）未做走查录屏（Web 同构，风险低）；
2. [低] AFG 其余 56 部 `m` 区间未补全（方法已文档化，见 08 号文档）；
3. [低] 「版原创」「小说改/游戏改」标签升级待业务确认；
4. [低] 草稿含密码字段存于本机与用户自己的云端（与既有敏感数据同级；如需更严可排除密码字段，改动点 `collectDraft`）。
