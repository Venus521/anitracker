# 06 · 迁移与回滚（v2.7.0）

## 一、本次变更文件清单（全部已留备份）

| # | 文件 | 变更 | 备份 |
|---|---|---|---|
| 1 | `index.html` / `ani-tracker.html` | +56,371B：注入 at270 模块（CSS+JS）；版本 2.6.1→2.7.0（build 20260916b） | `backup_20260916_pre-v270/` |
| 2 | `tracker-filler-data.js` | one-piece 全量更新：`f=115`、`m=43`（1168 集全覆盖）；文件头注释更新 | 同上 |
| 3 | `tracker-version.json` | 2.7.0 / 20260916b + 更新说明 | 同上 |
| 4 | `tracker-sw.js` | 缓存版本 `anitracker-v10` → `anitracker-v11` | 同上 |
| 5 | `tests/run-regression.js` | T01 版本断言 → 2.7.0 | 同上 |
| 6 | `tests/account-e2e.js` | T01 版本断言 → 2.7.0 口径 | 同上 |
| 7 | `tests/run-v270-tests.js` | **新增**（28 项专项测试） | — |

## 二、数据迁移（增量式）

- 全部为**新增字段/新增存储键**（`tOrig/tCn/at_drafts/at_relations/at_edit_log/at_fg_snap/at_v270`），无删除、无改写；
- 首次进入 v2.7.0：`at_v270` 旗标 + `at_fg_snap` 版本标记写入；若 Filler 数据版本变化，自动清 `at_fg_cache` 并跑一次批量校准（海贼王即刻受益）；
- 存量条目无需任何转换；云端同步为整包镜像，新键随包上传。

## 三、发布步骤（单实例灰度适配）

1. 本机验证（已完成：28/28 + 10/10 + 19/19 + 冒烟 + 审计/盲测）；
2. 手机浏览器打开入口（服务 0.0.0.0:8089，局域网可达），走一遍注册/录入；
3. 观察期 24–48h：「▦ 数据质量」面板的草稿数/台账数/覆盖率为主要指标；
4. SW 为 network-first，无需额外发版动作；缓存版本已升 v11。

## 四、回滚

**触发条件**：注册/录入出现阻塞性报错；联想/编辑影响既有功能；版本不一致。

**文件级回滚（30 秒）**：

```powershell
# 项目目录 D:\项目\01_媒体娱乐\ani-tracker 下：
Copy-Item "backup_20260916_pre-v270\index.html","backup_20260916_pre-v270\ani-tracker.html","backup_20260916_pre-v270\tracker-sw.js","backup_20260916_pre-v270\tracker-version.json" . -Force
```

- 回滚后：旧版本忽略新字段/新键（不报错），草稿/关联/台账数据保留在 localStorage 但无界面（无害）；Filler 数据更新对旧版同样生效（只会更准）；
- 恢复 v2.7.0：`_rollback` 暂存见工作台，或重新执行本包 `changes/` 注入脚本。

**回滚演练**：已执行（备份→回滚→哈希校验→恢复，见 05 号文档第三节的演练记录方式；本次沿用同一演练脚本模式）。

## 五、监控

- 「▦ 数据质量」面板：分类覆盖率、双语缺口、别名覆盖、台账/草稿计数——即本应用的运维指标；
- 测试三件套（`run-v270-tests.js` / `account-e2e.js` / `run-regression.js`）为发布前门禁；
- 云端指标（注册/登录成功率）沿用上一交付的通道（CloudBase 控制台 + 账号链路测试），本次未改动云侧。
