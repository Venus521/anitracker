# 05 · 验证记录与端到端走查（v2.4.4 · 2026-09-15）

## 一、自动化回归（tests/run-regression.js）— 16/16 PASS

运行方式：双击 `运行回归测试.bat` 或 `node tests/run-regression.js`；结果写入 `tests/last-regression.json`。

| # | 断言 | 结果 |
|---|---|---|
| T00 | 双入口字节一致（index.html === ani-tracker.html） | PASS |
| T01 | 页面版本与 tracker-version.json 一致（2.4.4） | PASS |
| T02 | bgmGet 死代码已移除；manifest start_url=index.html | PASS |
| T10 | token 绑定成功（模拟 /me） | PASS |
| T11 | 差量推送：远端收到 3 集已看 | PASS |
| T12 | 回滚后远端已看清零 + 台账「已回滚」 | PASS |
| T13 | 拉取全部：补标 2 集，本地 1-5 齐、评分回填 | PASS |
| T14 | 429 限流：失败入台账且可重试；重试后恢复 | PASS |
| T15 | 401：凭据失效入台账；手动添加/本地标记不受影响 | PASS |
| T16 | 404 下架：台账出现「可能已下架」 | PASS |
| T17 | 黑洞网络拉取全部报错 < 30s（修复前 63537ms） | PASS |
| T18 | 未开播条目：提示明确（srMsg+toast）且不入库 | PASS |
| T19 | 全程无页面级 JS 错误 | PASS |

覆盖的五类异常路径：断网（黑洞超时）、限流 429、token 失效 401、服务端错误（429/5xx 注入通道）、条目下架 404。

## 二、真实网络走查（无 token 匿名链路，真身 8091）

| 场景 | 结果 | 截图（docs/shots/） |
|---|---|---|
| 首页加载 v2.4.4/20260915b | 正常 | w1-home-v244.png |
| 搜索「孤独摇滚」双源 | 本地 1 + 在线 10 条 | w2-search-v244.png |
| 在线添加 + 标记 1 集 | 16 集入库、进度 6% | w3-detail-marked-v244.png |
| 未开播条目添加（新行为） | 「该条目暂无剧集数据（未开播或官方未录入）——暂时不能添加；可等更新后再试，或用「＋ 手动添加」先记录」+ toast，不入库 | w4-noepisodes-tip-v244.png |
| 同步中心未绑定（新文案） | 「未绑定 — 搜索免绑定可用；绑定后启用进度同步」+ 版本行 ✓ 一致 | w5-sync-unbound-v244.png |
| 假 token 校验 | 「验证失败：凭据失效…」且不落库 | w6-badtoken-v244.png |
| 台账 | 「添加条目 · 来源：在线搜索 · Bangumi #328609 ✓」 | w7-ledger-v244.png |

## 三、授权链路验证（模拟 Bangumi API · 127.0.0.1:8092）

绑定（/me 校验）→ 拉取全部（进度/状态/评分回填）→ 单集自动同步（PUT）→ 差量推送（PATCH）→ 回滚（恢复 prev 状态）→ 重试 → 401/404/429/hang 注入，全部通过（详见 evidence/audit-C*-results.json）。模拟器实现口径与 bgm.tv v0 开放 API 一致（状态映射 want1/done2/watching3/drop5、PATCH 批量、PUT 单集、202 建藏）。

## 四、回滚演练

备份目录（v2.4.3）→ `_rollback_drill_20260915/` 起服 8093：tracker-version.json=2.4.3/20260915a、index.html 807,233 字节正常伺服 → 演练通过。恢复步骤见 04 号文档。

## 五、数据安全核对

- 修复全程未触碰任何浏览器内用户数据（测试均在无痕 profile / 独立端口）；本地 tr_shows/at_sync_log 无结构变更，v2.4.3↔v2.4.4 双向兼容。
- 备份含 SHA256SUMS.txt，回滚后哈希可核对。
