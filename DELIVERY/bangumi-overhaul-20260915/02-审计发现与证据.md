# 02 · 审计发现与证据（2026-09-15）

> 测试环境：本机 Windows · 无头 Chrome（puppeteer-core）· 真身 127.0.0.1:8091 · 模拟 Bangumi API 127.0.0.1:8092 · 回归服务 8094 · 回滚演练 8093
> 证据文件：`DELIVERY/bangumi-overhaul-20260915/evidence/`（audit-A/B/B2b/C/C2/walkthrough 结果 JSON + 截图 PNG）

## 一、通过项（13 项 → 全部保留）

| # | 功能 | 实测证据（修复前基线，真网/模拟） |
|---|---|---|
| 1 | 免绑定双源搜索 | 真网搜「孤独摇滚」：本地 1 + 在线 10 条，~4.3s（audit-A） |
| 2 | 在线添加自动拉全集 | #328609 → 16 集 1.0s，含 bid 章节映射（audit-A） |
| 3 | 空条目一键补拉 | 横幅点击 526ms 补齐 16 集，拉取期间锁定防重复（audit-B） |
| 4 | 集标记状态机 | 标 1/2/13/14 → 刷新持久，下一集联动（audit-B） |
| 5 | token 绑定校验 | 合法→「已绑定：tester」；非法→「凭据失效」且不落库（audit-A/C） |
| 6 | 拉取全部（进度/状态/评分回填） | 补标 5 集 + 评分 8 回填（audit-C） |
| 7 | 单集自动同步 | 标记第 6 集 → mock 收到 PUT，台账当日合并（audit-C） |
| 8 | 差量推送 | 本地 3 集差异 → PATCH 3 集，0 差异时推 0（audit-C2） |
| 9 | 回滚 | 远端 3 集恢复未看，台账「已回滚 3 项」，新建收藏不可删有注明（audit-C2） |
| 10 | 限流重试 | 429 → 3 次退避重试 → 台账失败项 + 「重试失败项」按钮（audit-C） |
| 11 | 台账全留痕 | 添加/拉取/推送/单集自动/重试全部入账，80 条上限（audit-A/C） |
| 12 | 401 不拖垮本地 | 掉 token 后手动添加、本地标记正常（audit-C2） |
| 13 | 断网搜索降级 | 本地结果照常，最终出现降级文案（audit-B；延迟问题见 D3） |

## 二、缺陷项（7 项）

| ID | 级别 | 缺陷 | 证据 | 处置 |
|---|---|---|---|---|
| D1 | P0 | 双入口漂移：ani-tracker.html 停在 v2.4.0（20260914b），index.html 已 v2.4.3；manifest start_url 指向旧文件 → PWA/旧入口用户永远用旧版 | AT_VERSION 对比；manifest L4 | 整改：同步副本+同步脚本+回归锁定；start_url→index.html |
| D2 | P1 | 未开播条目（0 集）点添加：只改搜索页一行小字、无 toast；「该条目没有可用剧集」被拼进「拉取失败…可稍后重试」文案，误导成网络问题 | audit-B2b（srMsg/双无提示）+ audit-B | 整改：区分文案 + toast（v2.4.4 已修） |
| D3 | P1 | 黑洞网络下「拉取全部」63.5s 才报错：NET 类错误也重试 3 次 ×（直连 8s+两代理 6s×2） | audit-C2 r5hangMs=63537 | 整改：NET 不重试（限流/5xx 保留 3 次退避），代理 6s→5s（v2.4.4 已修，回归实测 <30s） |
| D4 | P2 | 绑定卡文案「绑定后启用搜索与同步」与 v2.4 免绑定搜索矛盾 | audit-A 同步中心截图 | 整改：改「搜索免绑定可用；绑定后启用进度同步」 |
| D5 | P3 | 404 文案「条目/章节不存在（映射歧义）」不知所云 | 代码 L1485 | 整改：改「可能已下架/改版，或本地映射过期」 |
| D6 | — | （撤销）门户状态探测——核查后确认已有 .catch，控制台 CORS 噪音为浏览器层日志，非代码缺陷 | 代码 L2356-2362 | 不动 |
| D7 | P3 | 死代码/遗留：bgmGet 无引用；ani-tracker-enhanced.js 未被引用；启动服务器.py 日志目录遗留 | grep + 代码 | 删除/归档/路径兜底（v2.4.4 已处理） |

## 三、关键网络实测（真网，2026-09-15）

- api.bgm.tv 直连 200（GET /v0/subjects/8，8.1KB）——本机网络可用。
- lain.bgm.tv 封面直连 200；proxy.cors.sh / api.codetabs.com 本机不可达（与 v2.4.3 记录一致）。
- 匿名搜索/详情/剧集接口均可用（CORS 全开）；鉴权接口对无效 token 返回 401。

## 四、假设与缺口（明示）

1. 无真实 Bangumi 测试账号/token：**授权写链路（收藏/进度写）用本地模拟 API 验证**；匿名读链路（搜索/详情/剧集/封面）用真实 api.bgm.tv 验证。用户提供真实 token 后可一键复验：`node tests/run-regression.js`（将拦截域名改回真实 API 或按文档操作）。
2. 评分推送链路经模拟验证（回填 8 分 + bgmSubjectBody 携带 rate）；真实账号首次同步建议先「拉取」再「推送」（见操作手册）。
