# 04 · 变更清单、发布与回滚说明（v2.4.4 / build 20260915b）

> 变更日期：2026-09-15 · 变更人：AutoClaw · 基线：v2.4.3（backup_20260915_pre-bangumi-fix/）

## 一、文件级变更（修复后 SHA256 前 16 位）

| 文件 | 变更 | 修复后 sha |
|---|---|---|
| index.html | 8 处修改 + bgmGet 删除；版本 2.4.4/20260915b | 23efb4777bec3e8d |
| ani-tracker.html | 同步为 index.html 副本（字节一致） | 23efb4777bec3e8d |
| tracker-version.json | version 2.4.4 / build 20260915b | — |
| tracker-manifest.webmanifest | start_url → ./index.html | — |
| tracker-sw.js | 缓存 anitracker-v7→v8 | — |
| 启动服务器.py | 日志目录兜底（防崩溃） | — |
| 同步双入口.py | 新增：index.html→ani-tracker.html 同步工具 | — |
| tests/（mock-bgm-api.js、run-regression.js、last-regression.json） | 新增回归套件 | — |
| 运行回归测试.bat | 新增一键回归 | — |
| _archive/ani-tracker-enhanced.js | 死文件归档 | — |

代码修改点（index.html）：
1. 版本号 2.4.4/20260915b
2. `_bget` 代理单跳 6000→5000ms
3. `bgmFetch`：仅限流/5xx 自动重试；网络类错误一次即报（63.5s→约 20s）
4. `bgmErrText` 404 文案改「可能已下架/改版，或本地映射过期」
5. `cacheCover` 封面拉取加 6s AbortController 超时
6. `bgmAttach` 失败分支：未开播（无剧集）独立提示 + toast；其余失败也补 toast
7. 绑定卡文案两处（免绑定搜索口径）
8. 删除无引用 `bgmGet`

## 二、发布步骤（照做即可）

1. 双击 `AniTracker追迹-一键打开.bat`（自动起服 8089 + 打开页面，普通刷新即见新版）。
2. 打开同步中心核对版本行：`页面 v2.4.4 · 服务端 v2.4.4 ✓ 一致`。
3. 跑一次回归：双击 `运行回归测试.bat`，期望 `SUMMARY: 16/16 PASS`（需 Chrome + Node，结果存 tests/last-regression.json）。
4.（可选）GitHub Pages 同步：提交本仓库并推送 main/gh-pages 后，线上 /anitracker/ 即为 v2.4.4。

## 三、功能开关与灰度

- 现有开关：同步中心 → 高级设置 →「免绑定搜索」（at_flags.searchNoSync，默认开；关闭即回退旧行为）。
- 本次无新增开关：所有修改为行为修复与文案，无接口变更；PWA/双入口双路径即天然灰度（index.html 与 ani-tracker.html 内容一致）。

## 四、回滚（已演练 ✅）

- 备份：`backup_20260915_pre-bangumi-fix/`（含 SHA256SUMS.txt，index.html=修复前 79C72DBF…前缀 v2.4.3 版本）。
- 演练记录：备份目录复制到 `_rollback_drill_20260915/` 并起服 8093 → tracker-version.json 返回 2.4.3/20260915a，index.html 807233 字节正常伺服（2026-09-15 16:5x 实测）。
- 恢复命令（在项目根目录执行）：
  ```
  copy /Y backup_20260915_pre-bangumi-fix\index.html index.html
  copy /Y backup_20260915_pre-bangumi-fix\ani-tracker.html ani-tracker.html
  copy /Y backup_20260915_pre-bangumi-fix\tracker-version.json tracker-version.json
  copy /Y backup_20260915_pre-bangumi-fix\tracker-manifest.webmanifest tracker-manifest.webmanifest
  copy /Y backup_20260915_pre-bangumi-fix\tracker-sw.js tracker-sw.js
  copy /Y backup_20260915_pre-bangumi-fix\启动服务器.py 启动服务器.py
  ```
  恢复后刷新页面即可（SW 为 network-first，无需强刷；如装了 PWA 建议关开一次）。
- 回滚触发条件：回归套件任一 FAIL、同步中心版本行不一致、真实账号同步出现数据异常。

## 五、最低限度监控

- 服务层：`服务器日志.txt`（请求日志 + 空闲自退/单实例记录）。
- 业务层：同步中心「同步记录」即操作台账（时间/对象/结果/失败原因/重试/回滚）；连续失败时失败项聚合在台账顶部，可直接「重试失败项」。
- 版本一致性：同步中心版本行自动比对页面与服务端版本。
- 建议（可选，未实施）：每次「拉取/推送全部」后人工扫一眼台账 fail 计数；如需告警可后续把 at_sync_log 尾部状态接入 WebDAV/云同步上报。
