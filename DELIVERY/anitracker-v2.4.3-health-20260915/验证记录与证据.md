# 验证记录与证据 · AniTracker v2.4.3 修复批次

> 本文件是“修复与复验”的过程台账：每条记录含 **目的 / 方法（可复跑）/ 关键结果 / 证据文件**。
> 证据文件均在 `verification/` 与 `assets/` 子目录；时间均为 2026-09-14/15（GMT+8）。

## 零、环境与基线

- 项目：`D:\项目\01_媒体娱乐\ani-tracker`；服务：8089（`服务器-空闲自退.py`）
- 修复前版本：v2.4.2 / build 20260914d（index.html SHA256 `79C72DBF…408CE`）
- 修复后版本：v2.4.3 / build 20260915a（index.html SHA256 `E6CCA129…954F5`）
- 工具：Node 22 + puppeteer-core（无头 Chrome）、curl、Python 3.13.12
- 备份点：`backup_20260914_pre-v243/`（5 文件 + 双 bat）

## 一、诊断阶段（修复前，复现“用不了”）

| # | 目的 | 方法 | 关键结果 | 证据 |
|---|---|---|---|---|
| D1 | 基础加载探针 | 无头浏览器加载（带 v / 不带 v） | 页面渲染正常；唯一错误 = favicon 404；版本 v2.4.2 | diag1 记录（会话台账） |
| D2 | 正常链路可通性 | 种入“空条目（已关联 Bangumi）”→ 点击“点这里自动拉取” | **成功**：1.2s 拉取 16 集；标记 + 刷新持久化 ✅（说明主路径健康） | `diag2-result.json` |
| D3 | **失败恢复能力（关键）** | 拦截模拟断网 → 点击拉取 → 失败后解除拦截、**不刷新**再点 | **复现缺陷**：即使网络恢复，重试仍失败；等待 22.2s 后报错 `signal is aborted without reason`（粘性状态 R1 实锤） | `diag3-result.json` |
| D4 | 缓存/刷新一致性 | 五种加载：带 v / 无参 / 普通刷新 / 绕缓存刷新 / 独立会话 | 版本与行为一致 ✅（缓存机制本身健康） | `diag4-result.json` |
| D4b | 渲染完整度 | 空态 / 有数据 / 详情页逐屏断言 | 渲染完整、选择器与文案核对无误 | `diag4b-result.json` |
| D5 | 外部依赖核查 | curl 直连与代理对照 | api.bgm.tv 直连被阻断（HTTP 000）；经代理 200 ✅；两个 CORS 兜底代理**全部不可达**（HTTP 000） | 会话日志（已并入报告 R2） |

## 二、修复阶段（写入并校验）

- 备份：`backup_20260914_pre-v243/`（7 文件，修复前哈希全量记录）
- 补丁：P1–P8（见《变更日志-问题对照》）逐条“定位校验 count=1 → 替换 → 写回”
- **静态校验全过**：编码（无 BOM、CRLF 一致）；3 个内联 JS 块 `node --check` ALL PASS；SW 语法 PASS；JSON 可解析；Python `py_compile` PASS；GBK bat 字节级比对 `bytes == expected: True`
- **终态总检**：19 项“应在场”标记全部 PASS，8 项“应清除”残留全部 PASS（final_check 输出）
- 修改后哈希：index.html `E6CCA129…954F5`（以交付快照为准）

## 三、复验阶段（修复后，证据编号 E#）

| 编号 | 目的 | 方法 | 关键结果 | 证据 |
|---|---|---|---|---|
| E4 | **总复验** | 单脚本五组断言：版本与缓存 / 正常拉取（含忙碌反馈采样）/ 标记持久化 / 断网失败 + **无刷新恢复** / 刷新一致性 | 版本 2.4.3、SW 缓存 `anitracker-v7`、favicon **200**；拉取 16 集 2.89s、忙碌提示 **329ms** 出现；**断网解除后 530ms 直接恢复**；普通与绕缓存刷新均 2.4.3 | `diag5-result.json` |
| E5 | **异常路径七场景** | 双击防重 / 手动改集数（有效+非法+取消）/ 从片单移除 / 超时模拟 / 拉取中途刷新 / 无参数加载 / 版本行 | 全部通过：双击仅 1 次请求；非法输入不生效；移除后回到列表；超时 15.3s 有界失败；中断后重拉成功 16 集；无参 2.4.3；版本行“页面 v2.4.3 · 服务端 v2.4.3 ✓ 一致” | `diag6-result.json` |
| E6 | **热更新可见性** | SW 已激活 → 修改 index.html 标题 → **普通刷新** → 还原再刷新 | 改动**立即可见**；还原后哈希一致（`shaRevertOk: true`）→ “不再依赖强刷/换参数”直接证据 | `diag7-hotupdate.json` |
| E7 | 门户按钮修复 | 修复前/后各一次真实点击 | 前：落地 404 错误页；后：落地门户搜索页（video-hub） | `portal-prefix-TEST.json` / `portal-postfix-TEST.json` |
| E8 | 干净环境 + 守卫 + 回滚 | ① 复制运行文件到新目录、8097 从零启动并复验；② 双启动守卫实测；③ 回滚演练（2.4.3→覆盖备份→2.4.2→恢复 2.4.3） | ① 版本 2.4.3、拉取 16 集、0 错误；② 第二实例自退、监听恒为 1、日志留痕；③ 回滚后 2.4.2、恢复后 2.4.3、文件哈希一致 | `clean-env-check.json` / `guard-occupied-log.txt` |
| E9 | 界面证据 | 无头浏览器截图 9 张 | 用户上报态 / 修复后拉取 / 恢复 / 失败提示 / 门户前后 / 干净环境 / 版本行 | `assets/shot-01…09` |
| E10 | 全量补丁 | unified diff 生成（5 文件） | 193 行、10.4KB | `changes-v2.4.3.diff` |
| E11 | 回滚点 | 目录快照 | 5 个核心文件 + 2 个 bat | `backup_20260914_pre-v243/` |
| E12 | 复跑脚本 | 全部诊断/复验脚本归档 | 均可重跑并输出 JSON 对照 | `verification/diag*.js` |

## 四、关键数值速查（抽查值）

- 正常拉取：**16 集 / 2.89s**（对应条目：Bangumi #328609）
- 断网失败提示：**0.28s**；解除断网后恢复：**0.53s**（不刷新）
- 忙碌反馈出现时间：**329ms**
- 超时场景有界失败：**15.3s**（上限设计 ≈20s）
- 双击防重：主体请求 **1 次**（对照：正常为 1 次）
- favicon：**404 → 200**；正常流程控制台错误：**1 → 0**
- 版本一致性：五种加载方式 **全部 2.4.3**；版本行“✓ 一致”

## 五、未覆盖 / 存疑项（如实登记）

- Firefox / Safari、macOS、Linux 未覆盖；
- 手机端真实网络环境未实测（仅覆盖桌面端与模拟）；
- Bangumi 账号推送 / 云同步的真实账号链路未端到端（需用户授权凭据），已验证其 UI 入口与失败提示路径；
- R11（SW 缓存条目累积）为观察项，未做长期压测。
