# AniTracker 追迹 · 项目健康体检报告（v2.4.3）

> 对「还是用不了 — 整个项目是否存在很大问题？」的系统性复盘、修复与验证
> 体检对象：http://127.0.0.1:8089/index.html ｜ 项目：D:\项目\01_媒体娱乐\ani-tracker
> 交付版本：v2.4.3（build 20260915a）｜ 日期：2026-09-15

## 一、核心结论

**不存在结构性大问题。**“用不了”由 4 处局部工程缺陷叠加所致——失败后整页粘死（R1）、失败等待过久且提示不清（R2）、拉取无持续反馈（R3）、门户按钮跳 404（R4）——**已全部修复，并经 7 组浏览器实测复验通过**。建议路径：**继续使用、小步维护（无需重建）**。

| 指标 | 数值 |
|---|---|
| 登记问题项 | 11（含 3 项设计特性说明） |
| 已修复（含实测证据） | 8 |
| 架构性隐患 / 数据丢失风险 | 0 |
| 验收标准复验通过 | 9/9（100%） |

核心链路（数据持久化、缓存机制、交互链路）经压测确认健康；本轮修复的是此前多次未覆盖到的“**失败后的恢复能力**”。

## 二、问题清单（R1–R11）

| 编号 | 级别 | 问题与根因 | 状态 |
|---|---|---|---|
| R1 | 高 | 失败后整页“禁用直连”粘性状态：网络恢复后不刷新重试仍失败（根因：全局标记一次失败永不恢复） | ✅ 已修复（零粘性） |
| R2 | 高 | 直连超时仅 3 秒 + 两个公共 CORS 兜底代理实测全部不可达；失败最长空等约 27 秒 | ✅ 已修复（8s + 6s×2，约 20s 封顶） |
| R3 | 中 | 拉取期间除 2.2 秒 toast 外无反馈，被误判“点了没反应” | ✅ 已修复（横幅锁定反馈） |
| R4 | 中 | “去门户播放”相对路径落在 8089 的 404 错误页 | ✅ 已修复（GW 基址 + 提示） |
| R5 | 低 | favicon.ico 404（控制台唯一持续错误） | ✅ 已修复 |
| R6 | 低 | 8089 双实例并存（快速连点启动器） | ✅ 已修复（单实例守卫） |
| R7 | 低 | 旧启动脚本与新版流程不一致（无 no-cache/无自退） | ✅ 已修复（委托新版） |
| R8 | 低 | 封面缓存走已失效代理；lain.bgm.tv 可直连 | ✅ 已修复（直连优先） |
| R9 | 信息 | 服务 15 分钟空闲自退 → 直访“连接拒绝”为设计行为 | 📄 已文档化 |
| R10 | 信息 | 直连 api.bgm.tv 被网络阻断，需系统代理；代理未开时拉取不可用 | 📄 已文档化 |
| R11 | 信息 | 随机 v 参数累积少量 SW 缓存条目（低影响） | 📄 仅登记 |

严重度分布：高 2 / 中 2 / 低 4 / 信息 3；修复成本折算约 2 人日（含诊断与全量复验）。

## 三、关键修复前后对照（实测）

| 场景 | 修复前（v2.4.2） | 修复后（v2.4.3） |
|---|---|---|
| 断网后重试 | 永久失败（除非刷新页面） | **0.53 秒直接恢复成功** |
| 拉取期间反馈 | 2.2 秒后无动静 | 横幅锁定“正在拉取…（约 20 秒上限）” |
| 失败文案 | signal is aborted without reason（不可读） | “网络不可达…请检查网络/代理后重试” |
| 去门户播放 | 落地 404 错误页 | 落到门户搜索页；未启动时明确提示 |
| 控制台 | favicon 404 长期存在 | 正常流程 0 错误 |
| 最坏失败等待 | 约 27 秒 | 约 20 秒封顶，且每次点击必重试直连 |

其他实测数据：正常拉取 16 集 2.89s；断网失败提示 0.28s；超时场景有界失败 15.3s；五种刷新方式（含无参数/禁缓存）行为一致；SW 已激活下改文件→普通刷新立即可见（哈希复核一致）。

## 四、验收标准复验矩阵（9/9 通过）

1. 首页可开可操作（带/不带 v）→ 通过（3 种 URL 形态实测）
2. 无阻断性错误 → 通过（正常流程 0 错误）
3. 整体健康结论 → 通过（本报告）
4. 每处修改对应根因 → 通过（变更日志 + 全量 diff）
5. 不依赖强刷/换参数 → 通过（热更新实测 + 五种刷新对照）
6. 数据真实存储可追溯 → 通过（标记回读一致；导出功能在位）
7. 异常路径可用 → 通过（七场景套件 + 端口占用）
8. 干净环境可复现 → 通过（新目录 8097 实测）
9. 启动/排障/回滚 → 通过（回滚演练：2.4.3→2.4.2→2.4.3）

## 五、交接要点

- 日常：双击 `AniTracker追迹-一键打开.bat` → 自动起服 + 打开页面；15 分钟空闲自动退出。
- 拉取依赖：需系统代理开启（Clash）；失败会给出明确提示，**开好代理直接重试即可，无需刷新/改参数**。
- 回滚：用 `backup_20260914_pre-v243\` 覆盖 3 个文件 + 重启（已演练）。
- 详见《运行与交接说明》《验证记录与证据》。

## 六、证据索引

E1 diag2-result.json ｜ E2 diag3-result.json ｜ E3 diag4-result.json ｜ E4 diag5-result.json ｜ E5 diag6-result.json ｜ E6 diag7-hotupdate.json ｜ E7 portal-TEST 前后 ｜ E8 clean-env + guard 日志 ｜ E9 截图 shot-01…09 ｜ E10 changes-v2.4.3.diff ｜ E11 backup_20260914_pre-v243 ｜ E12 diag*.js 复验脚本
（全部位于本交付目录，索引与说明同 HTML 版报告。）

---
修复后 index.html SHA256：`E6CCA129DD6F03C5A52E56880A541299C55350264A9DC5D183D1AC78703954F5`（修复前：`79C72DBF…408CE`）
