# 复核结论（S4 · reviewer gate）

## 独立复核员执行情况
- **复核员·标注与筛选（subagent_02）**：运行失败（启动约 1 秒即失败，无产出；`subagent_02.md` 未生成）。
- **复核员·同步与台账（subagent_03）**：超时（20 分钟上限，仅完成启动定位，无产出）。

→ 按集群纪律「每个 subagent 失败重试至多 1 次；再失败不重派——主线补位」处理，两项复核均**降级为主线的端到端测试证据承担**（证据强度见下）。

## 主线对质量门（plan.md 复核项 1-7）的逐条结论
| # | 复核项 | 结论 | 证据 |
|---|---|---|---|
| 1 | 集级标签混用 + 判定依据可查 | ✅ | E2E-1：详情同屏出现 原作改编/半原创/TV原创；标签 tooltip 带依据 |
| 2 | 每部番覆盖率可见 | ✅ | 列表覆盖行（原作x·TV原创y·半原创z·未判定w）+ 覆盖报告弹窗 |
| 3 | 筛选持久 / 一键恢复 / 排除≠已看 | ✅ | E2E-1：刷新保留、恢复全部、statuses 未被改动断言 |
| 4 | 下一集 / 进度 / 继续观看联动 | ✅ | E2E-1：隐藏后下一集 3→4→5；统计按可见集（2已看/5未看/29%） |
| 5 | 同步幂等 / 异常 / 回滚 / 重试 / 台账 | ✅ | E2E-2 全绿：差集推送、二次零写入、回滚恢复为0、500 重试、401 指引 |
| 6 | 版本对齐 / 无双跑 | ✅ | 页面=服务端 v2.3.0 ✓；旧路径 grep 为 0；47/47 请求命中模拟 |
| 7 | 原有功能回归 | ✅ | 历史记录 2 条保留、列表/搜索视图/返回正常、零 JS 错误 |

## 置信度
- **High**：以上全部为真实浏览器自动化断言（puppeteer + 无头 Chrome），同步链路以官方 OAS 为准绳做有状态模拟、覆盖 200/401/500 全路径。
- **Medium（未闭环项）**：① 真实 Bangumi 账号线上往返（本机无凭据；已给用户 4 步自查清单）；② 截图逐图人工核对（识图服务两次 500 不可用，降级为几何断言 15/15 + 截图留档）。

## 带瑕疵交付说明
- 复核员机制未产出独立第三方结论；主线自查 + 47 项断言为其替代，覆盖面完整、可复现（脚本与结果 JSON 均在 `docs/verification/`）。
- 无未修复缺陷遗留；无功能项被静默跳过。
