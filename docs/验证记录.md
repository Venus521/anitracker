# 验证记录（AniTracker v2.3 · 2026-09-14）

环境：本机 Windows · 无头 Chrome（puppeteer-core）自动化 · 测试服务器 `http://127.0.0.1:8091`（构建副本）与真身 `http://127.0.0.1:8089`。
Bangumi API 模拟：请求拦截 + 有状态 mock（覆盖 200 / 401 / 500 路径），节流按真实间隔执行。

## 一、功能测试 E2E-1 · 标注与筛选 —— 27/27 通过

- 迁移：旧默认 `canon` 被清除（不再静默按原作改编）✅；显式 semi 保留 ✅
- 来源筛选条：隐藏 TV原创 / 隐藏 半原创 / 批量补齐来源 / 覆盖报告 全部在位 ✅
- 列表覆盖信息（原作/TV原创/半原创/未判定）✅
- 同番混用标签可见：原作改编 + TV原创 + 半原创 同屏 ✅
- 覆盖行计数：原作 7 · TV原创 4 · 半原创 1 ✅
- 下一集：默认第 3 集（原创集也照常，仅"隐藏"后跳过）✅
- 标注编辑器：打开 / 写入 `e.src` / 标签即时更新 / 「未判定」=清除人工标注 ✅
- 隐藏联动：隐藏 TV原创后下一集跳第 4 集；双隐藏后跳第 5 集；已隐藏 4/5 集提示 ✅
- 持久化：刷新后筛选状态保留（✓ 隐藏 TV原创 + ✓ 隐藏 半原创）✅
- 统计口径：按可见集（已看 2 / 未看 5 / 29%）✅
- **被排除 ≠ 已看**：第 3 集保持未标记 ✅
- 一键恢复显示全部 ✅
- 未校准番全部「未判定」；纯原创番全部「TV原创」（系列推断）✅
- 批量补齐：AFG 命中写入（死神 → 32,49,64-109,128-137,…）✅；报告生成+弹窗 ✅
- 回归：添加页/返回/历史记录条数（2 条）保持 ✅
- 零页面级 JS 错误 ✅

## 二、同步链路测试 E2E-2 · 模拟 Bangumi API —— 23/23 通过

- 同步入口一次点击可达 ✅；绑定状态（/me）✅；**版本行：页面 v2.3.0 · 服务端 v2.3.0 ✓ 一致** ✅
- 拉取：补标 ep3、不误标 ep4、台账 ok ✅ —— 验证"看到第几集"的映射正确
- 单集自动推送（详情页点选）：写入台账 ✅（修复前此路径完全失效，见第五节）
- 整番推送：只推差集 `[1002]` —— 不重不漏 ✅
- 回滚：`[1002] → 0`，远端状态复原 ✅
- **幂等**：连续两次「推送全部」，第二次写入 0 条 ✅
- 一致性：本地已看 `[1,2,3,5]` = 远端 `[1001,1002,1003,1005]` ✅
- 拉取全部：1 成功 1 失败（500 → 分类为"触发接口限流"，可恢复）✅；失败项一键重试成功 ✅
- 401：台账给出「凭据失效——请在同步中心重新绑定 token」✅
- 双跑核查：无旧路径残留 ✅；47/47 请求全部命中模拟、零页面级 JS 错误 ✅

## 三、版面几何自检 —— 15/15 通过（视觉抽查的降级替代）

- 360 / 460 / 1280 三档宽度：无横向溢出（overflow=0）、顶栏按钮无重叠、同步入口可见、来源筛选条可见、列表覆盖信息正常。
- **降级说明**：原计划用识图服务逐张核对截图，识图服务两次返回 500 不可用（模型原生视觉通道亦不可用），故降级为「几何量化断言 + 截图留档」。11 张截图见 `docs/shots/`，建议人工过目一遍。

## 四、真身部署核验（8089）

- 服务器日志：`serving … (app v2.3.0)` ✅
- HTTP 下发 index.html 与磁盘哈希一致（`D35450CC…`）✅；`ani-tracker.html` 与 `index.html` 一致 ✅
- 真身页面冒烟：筛选条 ✅ / 同步入口 ✅ / 同步中心打开 ✅ / 版本行 ✓ 一致 ✅ / 零 JS 错误 ✅
- git：v2.3 提交（`68b9572`）已推送 `gh-pages` ✅（回滚点齐备）

## 五、根因修复清单（"同步没做好"的三个实锤）

| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | 单集推送使用已废弃路径 `PUT /users/-/collections/{subject_id}/episodes/{eid}` | 单集进度推送失败 | 改为官方 `PUT /users/-/collections/-/episodes/{eid}` |
| 2 | 详情页点选单集传字符串集号，与数字严格比较查不到 | **该路径下集永远推不上去** | 数值化比较 + 回归断言 |
| 3 | `_bget` 对 204 空响应体执行 `r.json()`，且任何失败都兜底三代理 | 写入操作被拖成 ~80s 长等待后失败 | 容错解析；代理兜底仅限网络层错误 |

## 六、未覆盖 / 留给你的最后一步（需要真实账号）

- 本机无可用 Bangumi 凭据，所有 API 行为以官方 OAS（2026-07-24）为准绳、以模拟环境逐路径覆盖；**线上真实往返请按以下 4 步自查**：
  1. 同步中心 → 粘贴你的 token → 「保存并测试」应显示"已绑定：<用户名>"；
  2. 点「拉取全部进度」，与 bgm.tv 网页端核对"看到第几集"；
  3. 标记一集后到 bgm.tv 收藏页核对单集状态；
  4. 在「同步记录」里试一次「回滚本次」。
- 建议先做一次：同步中心 → 本地备份 → 导出全部数据（存一份再操作）。

## 七、证据文件

- 截图：`docs/shots/`（e2e-01…07、live-01…04 共 11 张）
- 测试脚本（构建区）：`e2e1.js` / `e2e2.js` / `layout8089.js` / `smoke8089.js`
- 逐项结果 JSON：`e2e1-results.json` / `e2e2-results.json`
